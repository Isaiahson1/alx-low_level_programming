#include "main.h"

/**
 * _abs - compute the absolut value of an integer
 * @n: int to check
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	return (-n);
}
