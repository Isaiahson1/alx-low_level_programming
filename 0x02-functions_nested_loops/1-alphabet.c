#include "main.h"
/**
 * print_alphabet - prints all alphabet in lowercase
 *
 */

void print_alphabet(void)
{
	char letter;

	for (lerter = 'a'; letter <= 'z'; letter++)
	{
		putchar(letter);
	}
	putchar('\n');
}
